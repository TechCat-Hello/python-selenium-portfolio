[pytest]
markers =
    google_search: Google search related tests
    data_driven: Tests that use external data sources (CSV/JSON/Excel)
    csv: Tests that use only CSV data
    json: Tests that use only JSON data
    excel: Tests that use only Excel data